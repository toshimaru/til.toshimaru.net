---
title: 2026-06-20 Rails Enumerable#index_with / index_by
categories: rails
---

Rails の `Enumerable#index_with` は、配列などの要素をキーにして、ブロックの返り値を値にした Hash を作れる。

```ruby
%i[a b c].index_with(&:to_s)
# => { a: "a", b: "b", c: "c" }
```

逆に `Enumerable#index_by` は、ブロックの返り値をキーにして、要素そのものを値にした Hash を作る。

```ruby
%w[a b c].index_by(&:to_sym)
# => { a: "a", b: "b", c: "c" }
```

つまり、次の 2 つは同じ Hash になる。

```ruby
%i[a b c].index_with(&:to_s) == %w[a b c].index_by(&:to_sym)
# => true
```

## 参考

- [Enumerable#index_by \| RailsDoc(β)](https://railsdoc.github.io/classes/Enumerable.html#method-i-index_by)
- [Enumerable#index_with \| RailsDoc(β)](https://railsdoc.github.io/classes/Enumerable.html#method-i-index_with)
